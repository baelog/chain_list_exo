#include "exo1.h"

main_struct_t *feed_struct(int, int);

/* 
dans cette exercice le principe est simple metre {a} dans {varA} et {b} dans {varB}
et de retourné la stucture initial
*/

main_struct_t *feed_struct(int a, int b) // a replir
{
    
}

int main(void)
{
    main_struct_t *var = feed_struct(1, 2);
    verif(var);
}